# MarinaAI Command Hub — Feature Gap Register

Baseline date: 2026-08-21 · Checkout: `C:\Users\linde\Projects\MarinaAI` (repo `Marina-Command-Hub`)
Requirements source: Command Hub master requirements (Phases A–F, IA table, domain model, state machine, risk tiers) as specified in the project task brief; the three named prompt documents (`Cline_Prompt_MarinaAI_Command_Hub.md`, `Cline_Prompt_Command_Hub_Visual_and_Tool_Audit.md`, `MarinaAI_Command_Hub_Manus_Inspired_Feature_Inventory.md`) were **not found** in this checkout — requirements below are taken from the authoritative task text and verified against actual code.
Companion doc: `IMPLEMENTATION_SEQUENCE.md` (dependency-ordered plan) · Prior report: `LOCAL_RELEASE_READINESS.md`.

Status vocabulary: **implemented** (working + tested) · **partial** (works, incomplete vs requirement) · **scaffolded** (interface/seam only) · **not configured** (code exists, needs deployment/config) · **missing** (nothing exists) · **blocked** (needs an external decision).

| # | Capability area | Exact requirement | Actual code path / evidence | Status | Security / data / deployment dependencies | Existing tests & missing tests | Recommended next Cline task | Acceptance criteria |
|---|---|---|---|---|---|---|---|---|
| 1 | Workspace / auth / roles | Authenticated shell; User/Workspace/WorkspaceMembership (owner/admin/member/viewer); server-side tenant checks on every read/write | No auth anywhere. Single-operator local trust model. `workspaceId` field reserved in docs only; store is one JSON file (`dashboard-state.js`). Supabase client scaffolded in `web/src/lib/supabaseClient.ts` with placeholder fallbacks; `supabase/` has only `config.toml` | **missing** (auth), **scaffolded** (Supabase seam) | Requires Supabase project + RLS policies or equivalent; session handling; role model; every endpoint gains workspace scoping | None. Missing: auth unit tests, RLS policy tests, cross-workspace denial tests | Stand up durable auth+storage foundation (Supabase or Postgres adapter behind `server/store.js` seam); migrate JSON collections | Unauthenticated API returns 401; two workspaces cannot read each other's records by ID guessing; roles enforced server-side |
| 2 | Task creation | New Task composer: project, outcome, instructions, context refs, artifact type, priority, budget/time caps; client+server validation; audit event; navigate to detail | `TaskBoard.tsx` inline title-only form → `POST /api/tasks` → `createTask()` (title/owner/priority/status only). No composer, no context items, no budget fields, no task detail page | **partial** | Needs projects entity + validation schema; audit event on create | None for task creation. Missing: input-validation tests, audit-on-create test | Build compact New Task composer inside existing card rhythm + server-side validation + audit event | Composer persists validated task, writes audit event, navigates to task detail; invalid input rejected both sides |
| 3 | Plan generation | Server planner w/ provider adapter; strict-schema structured output (summary, assumptions, steps, deps, risk tier, approval flags); "provider not configured" honest state | Playbooks (`playbooks.js`) generate free-text markdown via askLLM — not schema-validated plans. No Plan entity persisted. Provider adapter exists (`agent.js`) with graceful degradation | **missing** (plan entity/schema), **partial** (provider adapter) | Provider key config; schema validator (zod already in web deps; none in agent) | None. Missing: planner schema-validation tests, not-configured-state test | Add Plan entity + deterministic dev planner + strict schema validation on provider output | Planner returns schema-valid plan or explicit not-configured error; malformed provider output rejected |
| 4 | Plan versioning / review | Versioned plans (draft/approved/superseded/rejected); edit→new version; request-revision keeps history; approve gates execution | State machine defines plan transitions (`server-state-machine.js`) incl. rejected→draft; **no Plan persistence, no versions, no review UI** | **missing** (persistence/UI), **implemented** (transition rules) | Storage layer; approval linkage | State-machine tests cover transitions. Missing: versioning persistence tests, revision-history tests | Persist plan versions keyed to tasks; add review actions to task detail | Editing creates v(n+1) draft; prior versions immutable; approval recorded with actor+timestamp |
| 5 | Task/run state machine | Explicit enforced transitions for Task/Plan/PlanStep/Run/Approval/Artifact/Automation; reject invalid; emit audit | `server-state-machine.js` fully implements all seven entities; **only Approval transitions are wired into live flows**; tasks still use free-form status strings (`createTask` accepts any status) | **partial** | Wire into store mutations; audit emission per transition | 8 transition tests pass. Missing: enforcement tests at API boundary | Enforce assertTransition() in createTask/completeTask/run paths; map legacy statuses | Invalid status change via API returns readable error + audit event; valid lifecycle passes end-to-end |
| 6 | Provider adapter | One configured model-provider adapter; env-configured; safe not-configured state; no unspecified endpoints | `agent.js` askLLM routes Gemini/Ollama/OpenAI-compatible/Copilot from config.json+env; timeouts set; falls back gracefully; `/api/health` exposes provider status truthfully | **implemented** (local), **not configured** (hosted: Vercel has no Ollama; keys absent) | API keys in config.json/env (present locally, redacted); hosted needs Vercel env vars | getLLMConfig covered indirectly. Missing: adapter timeout/fallback unit tests, not-configured UX test | Document + verify hosted provider configuration path; keep local Ollama default | Health endpoint reflects real provider availability in both environments; no secret leakage in responses |
| 7 | Queue / worker | Bounded executor runs on server/worker; cancellation checks between steps; usage accounting; bounded output | No queue. Playbooks run synchronously inside HTTP request (Vercel 60s cap noted in api/index.js). Scheduler = in-memory setInterval (`scheduler.js`), non-durable | **missing** (durable queue), **partial** (sync execution) | Durable worker (e.g., hosted cron/queue) before Automations; concurrency limits | None. Missing: queue enqueue/dequeue/cancel tests, timeout tests | Introduce job table + worker loop (can start in-process, must survive restart later) | Runs survive process restart; cancellation honored between steps; outputs size-bounded |
| 8 | Timeline / events | RunEvent stream: sequence numbers, typed names, safe summaries, redacted metadata, stable ordering | Only coarse `addTaskLog` entries (untyped strings, capped 12) and auditEvents. No per-run event stream, no polling/streaming endpoint for a run timeline | **partial** (audit exists), **missing** (run event stream) | Store capacity; redaction reuse | Audit lifecycle tests exist. Missing: run-event ordering tests, redaction-in-stream tests | Add runEvents collection + GET /api/runs/:id/events with seq ordering | Timeline shows ordered typed events per run; no credential material in any event |
| 9 | Cancellation / retry | Cancel honored between steps; retry creates linked attempt only for retryable failures; no auto-retry of denials | State machine defines cancel/retry paths (task failed/timed_out→queued; run same); **no runtime cancellation mechanism** (no job to cancel); playbooks cannot be cancelled mid-flight | **missing** (runtime), **implemented** (rules) | Queue dependency (#7) | Transition rules tested. Missing: mid-run cancellation integration test | Implement cancellation token checked between playbook steps; retry button creating linked run | Cancel during run stops before next step, records event; retry only offered for failed/timed_out |
| 10 | Artifacts / storage / provenance | Artifact entity: type, media type, storage ref, content hash, size, state, provenance lineage, retention | Artifacts = raw .md files in `tmp/` (73 present) served via `/api/reports/<name>` with path-traversal guard. No Artifact records, hashes, sizes, states, retention | **partial** (file serving), **missing** (entity/provenance) | Object storage or managed dir for durability (tmp/ is ephemeral on Vercel); hash computation | Report-serving guard untested. Missing: artifact CRUD tests, hash/integrity tests, cross-workspace access-denial test | Register artifacts on creation (hash+size+provenance) + Artifacts view listing real files | Every downloadable artifact shows source task/run, hash, created time; unauthorized access denied |
| 11 | Sources / research | Source cards: canonical URL/file ref, title, retrieval time, excerpt, trust labels; fetched content treated as untrusted | No Source entity; site monitor (`/api/site/monitor`) does health checks only; playbooks reference domains in prompts without fetching | **missing** | Allowlist of fetchable hosts; SSRF protections; size/type limits | None. Missing: fetch allowlist tests, untrusted-content handling tests | Add allowlisted research fetch tool producing Source records with provenance | Sources render with URL/time/excerpt/trust label; non-allowlisted hosts rejected; fetched content never executes |
| 12 | Policy / risk evaluation | Centralized policy engine pre-dispatch AND at execution; four risk tiers with minimum controls | `server-policy.js` implemented + wired into /api/chat (both checkpoints) and approval execution; tiers match spec; veto blocklist | **implemented** | Keep tier table current as tools grow | 7 policy tests pass. Missing: policy tests for future tools (research/browser/code) | Extend ACTION_RISK mapping as new tools land; add per-tool policy config | Every new tool dispatch passes through classify+veto twice; unknown actions fail closed |
| 13 | Approvals | JIT approvals: preview, risk, expiry, single-use, decision note, audit; centralized inbox; never "approve all" | Fully implemented: store + endpoints + ApprovalInbox UI + audit trail; TTL/single-use/binding verified by tests incl. HTTP-level | **implemented** | None blocking; multi-user requires approver identity (#1) | 6 approval unit tests + 2 HTTP gating tests pass. Missing: concurrent-decision race test | Add approver identity once auth lands; expiry sweeper for long-lived processes | Decisions carry actor identity; expired requests cannot execute; UI shows countdown |
| 14 | Audit trail | Append-only AuditEvent: actor, action, object, redacted before/after, correlation ID, timestamp | `auditEvents` collection + addAuditEvent/listAuditEvents + `/api/audit` + SecurityPanel view. Redaction enforced at call sites. No correlation/request IDs yet | **partial** | Retention policy; tamper-evidence (append-only is convention, not enforced) | Lifecycle tests assert event presence. Missing: correlation-ID propagation test, retention test | Add request/correlation IDs through API layer; define retention window | Consecutive events for one user action share a correlation ID; old events age out per policy |
| 15 | Tool registry | ToolDefinition: name/version/schemas/risk/approval policy/timeout/concurrency/redaction/flag/availability | No registry. Closest: ONE_CLICK_TOOL_DEFS (prompt strings) and KNOWN_PLAYBOOK_IDS (validation set) in playbooks.js; permissions.json is global not per-tool | **missing** | Registry becomes the policy source for #12; feature flags | Playbook-ID rejection tested implicitly. Missing: registry schema tests, per-tool limit tests | Create tool registry module; migrate playbooks + system actions onto it | Each tool declares schema/risk/limits; unregistered tool IDs rejected with clean error |
| 16 | Integrations / secrets | IntegrationConnection: provider, scopes, state, encrypted credential ref, owner, last-used, revoke; secrets server-side only | Gemini key stored plaintext in config.json (local file, single operator); ModelHub saves keys via /api/config; no connection entities, no revoke, no scope summary; LLM Hub view shows provider status honestly | **partial** (key mgmt), **missing** (connection model) | OS keychain/KMS or encrypted-at-rest store; never in client state | Config round-trip untested. Missing: secret-nonexposure tests (responses/logs), revoke flow tests | Move credentials to encrypted store behind server-only accessor; add Integrations view with real connection states | Secrets never appear in API responses/logs/client bundle; revoke stops usage immediately |
| 17 | Automation / scheduling | Durable schedules, idempotency, bounded retries, DLQ/review, per-workspace concurrency, pause/resume, templates | In-memory setInterval only (`scheduler.js`, 60s loop seeding ideas/summaries). Sidebar has no Automations entry; no schedule entities | **missing** (durable), **partial** (loop exists) | Hard dependency: durable queue/worker (#7) + storage (#1); browser-tab independence | Loop smoke-tested manually. Missing: schedule persistence tests, missed-run catch-up tests | After #7: Automation entity + scheduler reading due jobs from store | Schedules survive restart; missed runs handled idempotently; failing automations enter reviewable state |
| 18 | Browser / code workers | Isolated, scoped, time-limited browser & code execution; sensitive web actions need JIT approval; feature-gated until secure runner exists | Code exec exists but UNGATED shape: executor/runCommand.js runs arbitrary shell when permissions+env allow (currently double-gated off). No browser automation at all. No isolation (same process/host) | **missing** (isolation/browser), **blocked** (safe runner decision) | Container/VM runner; network egress policy; feature flag default-off | Veto blocklist tested. Missing: runner isolation tests, approval-gated browser tests | Decide runner strategy (container vs disabled); keep exec gated; scaffold browser-worker interface | No code/browser execution without explicit approval + isolation boundary; feature-flagged off by default |
| 19 | Multi-agent delegation | Orchestrated sub-agents with evaluation/review steps | Persona framing only ("Ava/Maya/Niko" in prompts); sequential single LLM calls; no orchestration, no eval steps | **missing** | Depends on queue (#7), tool registry (#15), eval harness | None. Missing: orchestration determinism tests, eval-gate tests | Defer until #7/#15 reliable; then add planner→executor→reviewer chain | Each delegated step produces reviewable output; evaluation gate can reject before completion |
| 20 | CTA / deployment routing | Authoritative production Hub destination; link-integrity CI check; no localhost in prod | Implemented + verified: commandHub.ts single source, /hub-access fallback, test:links passing, production build confirmed 2 CTAs→/hub-access. Pending: real HTTPS origin env var (deliberately unset) | **implemented** (mechanism), **not configured** (origin) | Vercel env var when domain confirmed | Link-integrity suite passing. Missing: CI wiring (GitHub Action) if desired | Set NEXT_PUBLIC_COMMAND_HUB_URL in Vercel once origin confirmed; optionally add GH Action running test:links | CTAs resolve to live HTTPS Hub; link check fails builds on regression |
| 21 | Mobile / accessibility | Responsive drawer nav; keyboard access; focus rings; text+color status redundancy; no horizontal scroll at 320px | Partially strong: focus-visible rings throughout, aria-labels on icon buttons, semantic buttons, grid collapses 1-col on mobile. Gaps: sidebar hidden <lg? (sidebar always rendered, fixed w-56 — cramped on small screens), no mobile drawer, tables (RunHistoryTable) may overflow, reduced-motion not handled (pulse/shimmer animations always on) | **partial** | None blocking | No automated a11y tests. Missing: keyboard-nav tests, 320px overflow checks, prefers-reduced-motion support | Add mobile drawer nav + reduced-motion media queries + overflow audit at 320px | All primary actions reachable at 320px without horizontal scroll; animations respect reduced-motion; full keyboard operability |
| 22 | Observability / backups / retention | Structured logs, metrics, backup of state, retention policy | Logs = console + in-memory log array (capped 10); system-metrics.js gives real OS telemetry; dashboard-state.json has NO backup; audit capped at 200 in-file; no retention jobs | **partial** (metrics/logs), **missing** (backup/retention) | Backup target (scheduled copy/object storage); retention config | Metrics endpoint used by UI. Missing: backup/restore test, retention pruning test | Add timestamped state backups + retention pruner + structured request logging | State restorable from backup; logs structured (JSON lines); retention prunes per policy |

## Summary counts
- **Implemented:** policy engine (#12), approvals (#13), CTA mechanism (#20), provider adapter local behavior (#6), state-machine rules portion of #5
- **Partial:** task creation (#2), state-machine enforcement (#5), timeline (#8), artifacts (#10), audit trail (#14), integrations (#16), scheduling loop (#17), mobile/a11y (#21), observability (#22)
- **Scaffolded:** auth/storage seam (#1)
- **Not configured:** hosted provider (#6), production Hub origin (#20)
- **Missing:** plan generation/versioning (#3/#4), queue/worker (#7), cancellation runtime (#9), sources/research (#11), tool registry (#15), automation durable (#17), browser/code workers (#18), multi-agent (#19)
- **Blocked:** safe code/browser runner strategy (#18) — needs an explicit infrastructure decision

## Cross-cutting notes
- Visual preservation constraint applies to every future task: changes stay within Panel/PanelItem/Card/Button rhythms; rapid-fire grid cadence is a defect trigger if altered.
- Security invariants already established and must hold through all future work: model output is data; high/critical → approval; destructive patterns vetoed; secrets never in responses/logs/client; MARINA_ENABLE_EXEC stays unset unless deliberately enabled.